The texts module
================

    >>> from pysyte.colours import texts

Leaving out colour name, is same as giving an empty one
    >>> assert texts.colour_text('Fred') == texts.colour_text('Fred', None) == texts.colour_text('Fred', 'none')

