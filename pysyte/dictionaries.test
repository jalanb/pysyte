The dictionaries module
=======================

    >>> import dictionaries
    >>> print(dictionaries.__doc__)
    Some methods to help with the handling of dictionaries

    >>> def method(key):
    ...     return key < 10 and 'small' or 'big'
    >>> stuff = dictionaries.LazyDefaultDict(method)
    >>> stuff[5] == 'small'
    True
    >>> stuff[198798] == 'big'
    True
    >>> print(stuff)
    {5: 'small', 198798: 'big'}
    >>> print(repr(stuff))
    {5: 'small', 198798: 'big'}
