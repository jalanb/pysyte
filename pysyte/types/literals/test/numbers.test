The numbers module
==================

    >>> from pysyte.types.literals import numbers
    >>> assert 'Handle numbers' in numbers.__doc__

Named numbers
-------------

The module names some numbers

    >>> assert numbers.ten == '10'
    >>> assert numbers.eleven == '11'
    >>> assert numbers.twelve == '12'

