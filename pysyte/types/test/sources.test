The sources module
==================

    >>> from rich import print
    >>> from pysyte.types.paths import path
    >>> from pysyte.types import sources
    >>> assert  'sources for pysyte' in sources.__doc__

    >>> source = sources.source(sources)
    >>> assert sources.source(path(sources)) == source
    >>> print(source.grep('def', 'source'))
