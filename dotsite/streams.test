The streams module
==================

    >>> import streams
    >>> print(streams.__doc__)
    Methods to handle streams

    >>> from cStringIO import StringIO
    >>> stream = StringIO()
    >>> with streams.swallow_stdout(stream):
    ...     print('Hello World')
    >>> stream.getvalue() == 'Hello World\n'
    True
